# Performance Tips

1. Prefer `filter()` over manual loops
2. Use dict lookups O(1) over list search O(n)
3. Memoize expensive recursive calls
4. Avoid creating large intermediate lists
5. Use `reduce()` for aggregation
